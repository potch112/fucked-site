---
layout: layouts/base.njk
---
<article>
  <h1>{{ title }}</h1>
  <p class="muted">{{ date | date("yyyy-MM-dd") }} · {{ fucked_level }}</p>
  <p class="lede">{{ lede }}</p>

  <section>
    <strong>Fucked.co.nz take</strong>
    <div class="satire">{{ content | safe }}</div>
  </section>

  <ul class="bullets">
    <li><strong>What’s fucked:</strong> {{ whats_fucked }}</li>
    <li><strong>What might unfuck:</strong> {{ what_might_unfuck }}</li>
    <li><strong>Odds of unfucking:</strong> {{ odds_unfucking }}</li>
  </ul>

  {% if source_url %}<p class="muted">Source: <a href="{{ source_url }}">{{ source_url }}</a></p>{% endif %}
</article>
