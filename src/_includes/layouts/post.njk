---
layout: layouts/base.njk
---
<article>
  <h1>{{ title }}</h1>
  <p class="muted">{{ date | date("yyyy-MM-dd") }} · {{ fucked_level }}</p>
  <p class="lede">{{ lede }}</p>

<section class="satire">
  {{ content | safe }}
</section>

<section class="facts">
  <div class="fact">
    <h3>What’s fucked</h3>
    <p>{{ whats_fucked | nl2br | safe }}</p>
  </div>
  <div class="fact">
    <h3>What might unfuck</h3>
    <p>{{ what_might_unfuck | nl2br | safe }}</p>
  </div>
  <div class="fact">
    <h3>Odds of unfucking</h3>
    <p>{{ odds_unfucking }}</p>
  </div>
</section>


  {% if source_url %}<p class="muted">Source: <a href="{{ source_url }}">{{ source_url }}</a></p>{% endif %}
</article>
