---
title: fucked.co.nz
layout: layouts/base.njk
---
<header class="center">
  <div class="badge">A very tidy catalogue of the thoroughly fucked</div>
  <h1>Welcome to <span>fucked.co.nz</span></h1>
  <p class="tagline">We recognise the obvious, organise the chaos, and keep it politely civil.</p>
</header>

<p class="muted">posts count: {{ collections.posts | length }}</p>

<section aria-label="latest">
  <h2>Latest</h2>

  {% set latest = collections.posts | limit(5) %}
  <ul class="postlist">
    {% for post in latest %}
      <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
        <span class="muted"> · {{ (post.data.date or post.date) | date("yyyy-LL-dd") }} · {{ post.data.fucked_level }}</span>
        {% if post.data.summary %}
          <div class="summary">{{ post.data.summary }}</div>
        {% endif %}
      </li>
    {% endfor %}
  </ul>

  <p class="muted"><a href="/archive/">View all posts →</a></p>
</section>
